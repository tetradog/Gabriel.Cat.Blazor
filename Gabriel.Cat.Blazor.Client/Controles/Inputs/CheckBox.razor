@*Gracias a la ayuda de Hazuky de es.StackOverFlow.com por su ayuda en el binding https://es.stackoverflow.com/questions/364370/problema-con-binding-en-blazor*@

    <Microsoft.AspNetCore.Components.Forms.EditForm Model="@this" >
        <Microsoft.AspNetCore.Components.Forms.InputCheckbox @bind-Value="Value"  style="display:inline"  @oninput="OnValueChanged" />
    </Microsoft.AspNetCore.Components.Forms.EditForm>
@code{

    [Parameter]
    public bool Value { get; set; }
    [Parameter]
    public EventCallback<bool> ValueChanged { get; set; }
        
    private Task OnValueChanged(ChangeEventArgs e)
    {
        Value = (bool)e.Value;

        return ValueChanged.InvokeAsync(Value);
    }
}
